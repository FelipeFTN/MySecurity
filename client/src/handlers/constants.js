export const LOCAL_HOST = "192.168.1.100";
export const SERVER_PORT = "8000";

export const INSTANCE_URL = `${LOCAL_HOST}:${SERVER_PORT}/instance`;
export const COMMAND_URL = `${LOCAL_HOST}:${SERVER_PORT}/command`;
export const AUTH_URL = `${LOCAL_HOST}:${SERVER_PORT}/auth`;